apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: campusconnect
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: <your-registry>/campusconnect-backend:latest
          ports:
            - containerPort: 8080
